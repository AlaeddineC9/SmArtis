{% extends 'base.html.twig' %}

{% block body %}
<style>
    /* Section titre */
    .hero-title {
        background: rgba(0, 0, 0, 0.4);
        padding: 16px;
        text-align: center;
        margin-top: 160px;
    }

    .hero-title h1 {
        font-size: 4em;
        font-weight: bold;
        margin: 0 auto;
        text-shadow: 2px 2px 5px rgba(0,0,0,0.3);
        background: linear-gradient(90deg, #F2DA5E, #D9961A, #F2D022);
        -webkit-background-clip: text;
        background-clip: text;
        color: transparent;
    }

    /* Bloc vidéo + intro côte à côte */
    .about-top {
        display: flex;
        flex-wrap: wrap;
        max-width: 1200px;
        margin: 60px auto 0;
        gap: 40px;
        padding: 0 24px;
    }

    .about-video {
        flex: 1;
        min-width: 300px;
    }

    .about-video video {
        width: 100%;
        height: auto;
        border-radius: 10px;
        display: block;
    }

    .about-intro {
        flex: 1;
        min-width: 300px;
    }

    .about-intro h2 {
        font-size: 2.8em;
        margin-bottom: 20px;
        color: #222;
    }

    .about-intro p {
        font-size: 1.4em;
        color: #555;
        line-height: 1.8;
        text-align: justify;
    }

    /* Reste du texte */
    .about-rest {
        max-width: 1000px;
        margin: 40px auto 80px;
        padding: 0 24px;
    }

    .about-rest p {
        margin-bottom: 20px;
        font-size: 1.5em;
        color: #555;
        line-height: 1.8;
        text-align: justify;
    }

    /* Responsive mobile */
    @media (max-width: 768px) {
        .hero-title h1 {
            font-size: 24px;
        }
        .hero-title {
            padding: 16px;
            margin-top: 60px;
        }

        .about-top {
            flex-direction: column;
            gap: 0;
        }

        .about-video,
        .about-intro {
            width: 100%;
            margin-bottom: 0;
        }

        .about-intro h2 {
            font-size: 2em;
            margin-top: 20px;
        }

        .about-intro p,
        .about-rest p {
            font-size: 1.2em;
            margin-bottom: 16px;
        }

        .about-rest {
            margin-top: 20px;
        }
    }
</style>

<!-- Titre principal -->
<div class="hero-title">
    <h1>Découvrez l’univers unique de SmArtis !</h1>
</div>

<!-- Bloc vidéo + intro -->
<div class="about-top">
    <div class="about-video">
        <video autoplay muted loop playsinline>
            <source src="{{ asset('uploads/videoSA.mp4') }}" type="video/mp4">
            Votre navigateur ne supporte pas les vidéos.
        </video>
    </div>
    <div class="about-intro">
        <h2>À propos de SmArtis</h2>
        <p>
            SmArtis, c’est bien plus qu’une marque :<br> Une philosophie où l’art rencontre l’intelligence créative pour sublimer le quotidien.<br>
            En récupérant des éléments issus de la nature ou d’associations, SmArtis redonne vie à des objets oubliés en leur offrant une nouvelle identité.
        </p>
    </div>
</div>

<!-- Texte complémentaire -->
<div class="about-rest">
    <p>
        Chaque création est artisanale, guidée par les émotions, et devient une pièce unique porteuse de sens.
        SmArtis incarne un engagement social : aider, créer avec passion, et offrir des objets qui véhiculent un message positif.
    </p>
    <p>
        Notre site vous invite à explorer nos créations, pensées pour être fonctionnelles ou décoratives, accompagnant vos moments marquants :
        mariages, baptêmes, anniversaires, etc.
    </p>
    <p>
        Et si vous ne trouvez pas votre bonheur ? Partagez-nous vos rêves et projets :
        nous les réaliserons avec soin et créativité.
    </p>
</div>
{% endblock %}